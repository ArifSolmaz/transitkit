# Read the Docs configuration file
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
```

And make sure `docs/requirements.txt` exists:
```
sphinx>=6.0
sphinx-rtd-theme>=1.2
myst-parser>=1.0
numpy
astropy